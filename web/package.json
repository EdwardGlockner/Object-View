{
  "name": "object-view-web",
  "version": "1.0.0",
  "description": "Small web layer for the ObjectView C++ backend.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "^7.1.12"
  }
}
